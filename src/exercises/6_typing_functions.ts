/* Tipado en funciones
Crea una función greet que tome un argumento name de tipo string y devuelva una frase de saludo usando ese nombre. */


function greet(name: string) {
    return `Welcome ${name}, to code with ts!`;
}