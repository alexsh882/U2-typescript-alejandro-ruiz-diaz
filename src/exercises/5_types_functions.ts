/* Función con tipado
Escribe una función add que acepte dos números como argumentos y devuelva su suma. 
Asegúrate de tipar adecuadamente los parámetros y el valor de retorno. */

// Declaro una función add que recibe dos parámetros de tipo number y devuelve un valor de tipo number.
function add(a: number, b: number): number {
  return a + b;
}